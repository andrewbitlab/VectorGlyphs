# Deployment Notes — VectorGlyphs

See the root `DEPLOYMENT.md` for the high-level deployment plan.

This folder-level document should eventually contain operational runbooks, exact host paths, Docker commands, health checks, backup notes, and rollback steps.

Phase 0 creates no deployment resources.
